version: '3.8'

services:
  stremio-import:
    build: .
    container_name: stremio-import
    restart: unless-stopped
    environment:
      - EMAIL=${EMAIL}
      - PASSWORD=${PASSWORD}
      - SCHEDULE_SECONDS=${SCHEDULE_SECONDS:-7200}
